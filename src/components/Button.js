import React from 'react';
import { Text, TouchableOpacity } from 'react-native';

const Button = () => (
  <TouchableOpacity>
    <Text>Buy Now</Text>
  </TouchableOpacity>
);

export default Button;
